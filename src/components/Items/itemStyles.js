import { makeStyles } from "@material-ui/core/styles";

export const itemStyles = makeStyles({
  root: {
    maxWidth: 345,
    marginRight: 40,
    marginBottom: 40,
  },
  media: {
    height: 240,
  },
  image: {
    maxWidth: 90,
  },
});
